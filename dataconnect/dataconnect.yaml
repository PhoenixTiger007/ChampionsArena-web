specVersion: "v1beta"
serviceId: "champions-arena"
location: "us-central1"
schema:
  source: "./schema"
  datasource:
    postgresql:
      database: "fdcdb"
      cloudSql:
        instanceId: "champions-arena-fdc"
      # schemaValidation: "COMPATIBLE"
connectorDirs: ["./connector"]
